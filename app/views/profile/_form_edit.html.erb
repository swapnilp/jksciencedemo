<div id="profile">
  <h2>Edit Profile</h2>
  <%- if current_user.role.name == "student" %>
    <%= form_for @student, :url => profile_path(@student), :method => :put, :remote => true,  :html => { :class => "form-horizontal"} do |f| %>
    
    <%- if @student.errors.any? %>
    <%- @student.errors.full_messages.each do |msg|%>
    <%= "#{msg}" %><br />
    <% end %>
    <% end %>
    
    <span class="row"><label>Address
	<%= f.text_field :address, :class => 'input-large' %>
</lable>
</span>


<span class="row"><label>phone_number
    <%= f.text_field :phone_number %>
</lable>
</span>

<span class="row"><label>standerd
    <%= f.text_field :standerd %>
</lable>
</span>
<%= f.submit "update", :class => 'btn btn-large' %>
<% end %>
<span class="label label-info">Please Contact to Admin  for change Ramaining Information</span>

<% end %>

<%- if current_user.role.name == "Admin" %>

  <%= form_for @admin, :url => profile_path(@admin), :method => :put, :remote => true,  :html => { :class => "form-horizontal", :id => "admin_subject" } do |f| %>
  <%- if @admin.errors.any? %>
  <%- @admin.errors.full_messages.each do |msg|%>
  <%= "#{msg}" %><br />
  <% end %>
  <% end %>
  <table width="100%" class="table">
    <tr>
      <td width="20%">Admin Name</td>
      <td><%= f.text_field :name %></td>
    </tr>
    <tr>
      <td>Phone Number</td>
      <td><%= f.text_field :phone_number %></td>
    </tr>
    <tr>
      <td>Email</td>
      <td><%= @admin.email %></td>
    </tr>
  </table>
  <%= f.submit "update", :class => 'btn btn-large' %>
  <%= link_to "Back", profile_path(current_user.id), :class=> 'btn btn-large', :remote => :true %>
  <% end %>
<% end %>

<%- if current_user.role.name == "teacher" %>
<% end %>
</div>
